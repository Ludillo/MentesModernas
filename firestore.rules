rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All application data passes through the authenticated Worker API.
    match /{document=**} { allow read, write: if false; }
  }
}
